import Sidebar, { Logo, SideButtons } from "../components/sidebar";

export default function NavSidebar() {
    return (
        <Sidebar>
            <Logo name="CA$H" slogan="Ticket to Riches"/>
            <SideButtons />
        </Sidebar>
    )
}